@model DungeonMentor.Models.TrainingResult

@{
    ViewData["Title"] = "Модуль завершён";
    var passed = Model.Score >= 80; // можно изменить порог
}

<h1>Обучение завершено</h1>

@if (passed)
{
    <div class="alert alert-success">
        <strong>Поздравляем!</strong> Вы успешно завершили модуль <strong>@Model.ModuleName</strong>.
    </div>
}
else
{
    <div class="alert alert-danger">
        <strong>К сожалению, вы не прошли модуль <strong>@Model.ModuleName</strong>.</strong><br />
        Попробуйте ещё раз, изучив материалы внимательнее.
    </div>
}

<p><strong>Оценка:</strong> @Model.Score / 100</p>
<p><strong>Дата прохождения:</strong> @Model.PassedAt.ToLocalTime().ToString("dd.MM.yyyy HH:mm")</p>

<a class="btn btn-primary" asp-action="Index">Назад к обучению</a>
<a class="btn btn-secondary" asp-action="History">Моя история</a>
