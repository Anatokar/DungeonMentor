@model List<DungeonMentor.Models.TrainingModule>

@{
    ViewData["Title"] = "Обучающие модули";
}

<h1>Обучающие модули</h1>

@foreach (var module in Model)
{
    <div>
        <h2>@module.Title</h2>
        <p>@module.Description</p>
        <a href="/Training/Module/@module.Id">Перейти к модулю</a>
    </div>
    <hr />
}